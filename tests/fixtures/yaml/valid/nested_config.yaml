database:
  connection:
    host: localhost
    port: 5432
    credentials:
      username: admin
      password: secret
  pool:
    size: 10
    timeout: 30

api:
  endpoints:
    users:
      path: /api/users
      methods: [GET, POST]
    products:
      path: /api/products
      methods: [GET, POST, PUT, DELETE]


