# pylight Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-11-21

## Active Technologies
- Python 3.11+ + Starlette, Uvicorn, SQLAlchemy, Strawberry GraphQL, PyJWT, python-jose, redis-py, PyYAML, Alembic, Click (001-pylight-framework)
- SQLAlchemy-compatible databases (PostgreSQL, MySQL, SQLite) (001-pylight-framework)
- PostgreSQL database (via Docker container at localhost:5432) (002-integration-examples)
- Python 3.11+ + None (cleanup task, no new dependencies) (003-cleanup-repository)
- N/A (file system operations only) (003-cleanup-repository)
- Markdown (GitHub-flavored) + None (documentation only, no code dependencies) (004-framework-readme)
- N/A (single README.md file) (004-framework-readme)
- N/A (configuration only, no persistent storage) (006-yaml-table-config)
- Bash shell scripting (POSIX-compliant), Python 3.11+ (for Pylight framework) (007-yaml-local-testing)
- N/A (testing infrastructure only, no persistent storage) (007-yaml-local-testing)
- PostgreSQL (via async SQLAlchemy) (008-fix-yaml-config)
- Python 3.11+, Markdown (009-github-pages-docs)
- Markdown files in repository, GitHub Pages hosting (009-github-pages-docs)
- External database (PostgreSQL, MySQL, or SQLite) - provided by user, not deployed by char (001-helm-deployment)
- Bash (shell scripts), YAML (Helm templates), Python 3.11 (integration tests) + Helm 3.0+, kubectl, minikube, Docker, PostgreSQL (Docker image), Redis (Docker image) (001-simplify-helm-testing)
- N/A (testing infrastructure uses ephemeral Kubernetes resources) (001-simplify-helm-testing)
- YAML (Helm templates), Bash (CI/CD scripts), Python 3.11+ (Pylight application) + Helm 3.0+, Kubernetes 1.20+, Docker, GitHub Actions, kind/minikube (for testing) (001-helm-chart)
- N/A (chart configuration only; database is external) (001-helm-chart)
- YAML (GitHub Actions workflows), Shell scripting, Python 3.x (for any automation scripts) + GitHub Actions, Docker, Helm 3.x, kind (Kubernetes in Docker), kubectl (001-automated-publish)
- GitHub Pages (gh-pages branch) for Helm chart repository, Docker Hub for container images (001-automated-publish)
- Python 3.11+ + Starlette (ASGI framework), SQLAlchemy 2.0+ (ORM), asyncpg/psycopg2-binary (PostgreSQL drivers), strawberry-graphql (GraphQL), pytest (testing), requests (HTTP client for tests) (001-fix-external-db-tests)
- PostgreSQL (external test database: pylight_test with 1000+ records per table) (001-fix-external-db-tests)
- PostgreSQL 15+ (dockerized) (001-populate-test-database)

- Python 3.11+ + FastAPI, SQLAlchemy, Strawberry GraphQL, WebSockets, PyJWT, python-jose, redis-py, PyYAML, Alembic, Click (001-pylight-framework)

## Project Structure

```text
src/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Python 3.11+: Follow standard conventions

## Recent Changes
- 001-populate-test-database: Added Python 3.11+
- 001-fix-external-db-tests: Added Python 3.11+ + Starlette (ASGI framework), SQLAlchemy 2.0+ (ORM), asyncpg/psycopg2-binary (PostgreSQL drivers), strawberry-graphql (GraphQL), pytest (testing), requests (HTTP client for tests)
- 001-external-database-test: Added Python 3.11+


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
